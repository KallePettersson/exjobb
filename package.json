{
  "scripts": {
    "start": "set NODE_ENV=development&& node index.js",
    "start-prod": "set NODE_ENV=production&& node index.js",
    "client-install": "cd edubytes-client && npm install",
    "server": "nodemon server.js",
    "client": "npm start --prefix edubytes-client",
    "client-build": "npm run build --prefix edubytes-client",
    "proxy": "localhost:3000"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "express-validator": "^6.14.0",
    "jsonwebtoken": "^8.5.1",
    "md5": "^2.3.0",
    "mongoose": "^6.1.8",
    "nodemon": "^2.0.16"
  },
  "engines": {
    "node": "16.13.0"
  }
}
